# Python 3.10+ required (mcp SDK dependency)
# Install: pip install -r requirements.txt

httpx>=0.27.0           # HTTP client for Admin GraphQL and Catalog MCP (raw transport)
anthropic>=0.40.0       # Mutation proposals + prompt generation
click>=8.1.0            # CLI for all pipeline scripts
pyyaml>=6.0.1           # Prompt set YAML files
rich>=13.7.0            # Terminal dashboard
python-dotenv>=1.0.0    # Load .env credentials
jinja2>=3.1.4           # HTML dashboard template (optional)

# Note: the mcp Python SDK is NOT required.
# catalog_client.py speaks the MCP Streamable HTTP protocol directly via httpx,
# which avoids the Python 3.10+ constraint of the mcp package.
